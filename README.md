# SOCKSY

SOCKSY e-commerce landing page converted from the original HTML design to React + Vite.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Place product/category images inside `public/images/` using the filenames referenced by `src/App.jsx`.
